// types: test_primitive_init
test "primitive_struct_init" {
    let a = int{};
    let b = int{5};
    let c = float{3.14};
    let d = i32{10};
    
    assert a == 0;
    assert b == 5;
    assert c > 3.13 && c < 3.15; // Float approximation check to use 'c'
    assert d == 10;
}
